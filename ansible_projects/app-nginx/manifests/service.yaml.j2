apiVersion: v1
kind: Service
metadata:
  name: {{app_name}}
  namespace: {{namespace}}
spec:
  type: {{service_type}}
  selector:
    app: {{app_name}}
  ports:
    - port: {{service_port}}
      targetPort: {{container_port}}
